/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.html",      // any HTML in the root
    "./src/**/*.css", // optional, if you use @apply
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}